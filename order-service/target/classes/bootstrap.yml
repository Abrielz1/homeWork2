server:
  port: 0
spring:
  cloud:
    enabled: true
    uri: http://localhost:8888/
    discovery:
      enabled: true
      service-id: discovery
  application:
    name: order-service
  profiles:
    active: order-service-local



eureka:
  client:
    enabled: true
    service-url:
      defaultZone: "http://localhost:8888/eureka"
